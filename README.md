## TODO
- Convert to OpenNext
  - Look into caching? Otherwise done I think
- Add testing framework, write tests
- Fix bug where non-null nullable fields don't get updated when patched with null values